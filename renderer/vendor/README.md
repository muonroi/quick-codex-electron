This directory holds vendored UMD bundles for xterm.js so the renderer can load without a bundler.

Populate by running:

```bash
cd quick-codex/apps/qc-electron
npm install
node ./scripts/vendor.mjs
```

